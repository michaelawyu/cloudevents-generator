events:
  http:
    attributes:
      type:
        type: string
        default: 'test type'
      source:
        type: string
        default: 'test source'
      data:
        type: array
        items:
          type: string
